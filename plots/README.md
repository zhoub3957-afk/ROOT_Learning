# Plots

This directory stores representative figures generated by the ROOT lessons.

Naming convention:

```text
lessonXX_<description>.<ext>
```

Keep source code in `lessons/` and generated visual outputs in `plots/` so that code and results remain easy to distinguish.

Large or intermediate data products should not be committed unless they are intentionally part of the reproducible project.
